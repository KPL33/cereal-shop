{
  "name": "cereal-shop",
  "version": "1.0.0",
  "description": "Cereal Shop App",
  "main": "server/server.js",
  "scripts": {
    "start": "cd server && node server.js",
    "build": "cd client && npm run build",
    "postinstall": "cd client && npm install && npm run build && cd ../server && npm install"
  },
  "dependencies": {
    "nodemailer": "^6.9.13"
  },
  "engines": {
    "node": "14.x"
  }
}
